# 07-pcap-lab-files

实验用 **PCAP** 存放目录（配合 NotebookLM / 本地 Wireshark 打开）。

## 规范

| 规则 | 说明 |
|------|------|
| **命名** | `ch07-arp-lab.pcap`、`ch08-tcp-handshake.pcap` 等，与章节对应 |
| **Git** | 默认 **不提交** 大文件（见 [.gitignore](./.gitignore)） |
| **上传** | NotebookLM 可单独上传小 PCAP；过大请本地分析 |

## 建议清单（待自备）

- [ ] `ch07-arp.pcap` — ARP 请求/应答
- [ ] `ch08-tcp-3way.pcap` — TCP 三次握手
- [ ] `ch09-dns-http.pcap` — DNS + HTTP
- [ ] `ch11-retransmit.pcap` — 重传/慢网络

→ [全书导航](../study.md)

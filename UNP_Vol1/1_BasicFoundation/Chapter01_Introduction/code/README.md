# Chapter 1 源码目录说明

| 路径 | 说明 |
|------|------|
| `code/{节名}/original_c/` | 原书 C 示例；**新手向逐行中文注释**（主副本） |
| `code/{节名}/rewrite_go/` | 日后 Go 重写占位 |
| `code/{节名}/rewrite_rust/` | 日后 Rust 重写占位 |
| `intro/` | 与 `original_c` 同步的短路径镜像，便于笔记链接 |

当前已有注释的 `.c`：仅 **1.2 / 1.3** 三套 `daytimetcpcli*`。以后每归档一节书上的 C 代码，会按同样风格补注释。
